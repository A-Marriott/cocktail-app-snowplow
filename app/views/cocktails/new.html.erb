<%= link_to 'home', root_path, class: 'btn btn-flat m-3' %>

<div class="container" style="padding: 20px 0;">
  <%= simple_form_for @cocktail do |f| %>
    <%= f.input :name %>
    <%= f.input :image, as: :file %>
    <%= f.submit "Submit cocktail", class: "btn btn-flat" %>
  <% end %>
</div>
